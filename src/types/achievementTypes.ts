// Definovanie interface pre Achievement
export interface Achievement {
    name: string;        // Názov achievementu
    description: string; // Popis achievementu
    earned: boolean;     // Stav, či bol achievement získaný alebo nie
  }
  